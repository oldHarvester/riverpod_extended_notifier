name: riverpod_extended_notifier
description: "A new Flutter package project."
version: 0.0.3+3
homepage:
publish_to: none

environment:
  sdk: ^3.2.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ">=2.0.0 <3.0.0"
  riverpod: ">=2.0.0 <3.0.0"
  equatable: ">=2.0.0 <=3.0.0"
  flutter_toolkit:
    git:
      url: https://github.com/oldHarvester/flutter_toolkit.git
  flexible_internet_checker:
    git:
      url: https://github.com/oldHarvester/flexible_internet_checker.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: